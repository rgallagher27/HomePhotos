version: "2"
sql:
  - engine: "sqlite"
    queries: "queries"
    schema: "migrations"
    gen:
      go:
        package: "sqlite"
        out: "."
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
